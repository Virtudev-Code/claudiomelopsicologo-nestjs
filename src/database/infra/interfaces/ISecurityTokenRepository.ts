// import SecurityToken from 'src/database/typeorm/SecurityToken.entities';
// import User from 'src/database/typeorm/User.entities';

// export default interface IUserTokensRepository {
//   generate(user_id: string): Promise<SecurityToken>;
//   findByToken(token: string): Promise<SecurityToken | undefined | null>;
//   findUserById(email: string): Promise<User | undefined>;
// }
